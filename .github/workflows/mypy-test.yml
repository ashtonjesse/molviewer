name: mypy test

on:
  push:
    branches: [ master ]

jobs:
  run_mypy:
    runs-on: ubuntu-latest
    name: Mypy
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run Mypy
        uses: jashparekh/mypy-action@v2
        with:
          path: '.\src\molviewer\'

